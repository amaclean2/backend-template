module.exports.validatePost = body => {
    switch(true) {
        default :
            return true;
    }
}

module.exports.validatePut = body => {
    switch(true) {
        default :
            return true;
    }
}

module.exports.validateDelete = body => {
	switch(true) {
		default :
			return true;
	}
}